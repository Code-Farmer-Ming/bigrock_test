page.visual_effect :fade, dom_id(@pass, "container"), :duration => 0.5
page.delay(0.5) do
  page[dom_id(@pass, "container")].remove
  page << "if ($('summary_info')!=null) {"
  page[dom_id(@pass, "summary")].remove
  page << " }"
end
