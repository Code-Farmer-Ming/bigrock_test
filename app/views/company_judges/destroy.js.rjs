page.replace_html("company_star_rating",render(:partial=>"companies/show_star_rating",:object=>@company_judge.company))
page.replace_html(dom_id(@company_judge.company,"operation"),render(:partial=>"companies/operation",:object=>@company_judge.company))

page.select( "##{dom_id(@company_judge,"judge_item")}" ).each  do |item|
  item.remove
end

page.select("#company_judge_count").each do |item|
  page.replace_html("company_judge_count",render(:text=>"评价信息" +(@company_judge.company.judges.size>0 ? "(#{@company_judge.company.judges.size})" : "")))
end