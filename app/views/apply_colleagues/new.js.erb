<div class="form_info">
  <h1>申请同事</h1>
  <% remote_form_for(@add_application ,:url=>apply_colleagues_path(:user_id=>params[:user_id])) do |form| %>
  <%#*<p>%>
  <p>你们在下列公司一起工作过:</p>
    <%current_user.need_comfire_colleagues.find_all_by_colleague_id(params[:user_id]).each do |undetermined|%>
      <h6><%=  "#{undetermined.my_pass.company.name}" -%></h6>
    <%end%>

  <%#= label_tag :name,"对",:class=>"filed_lab" %>
  <%#= label_tag "user_name", @respondent.name %>
  <%#*</p>%>

    <p>
      <%= form.label :memo,"说点什么吧",:class=>"1" %>
    </p>
    <p>
      <%= form.text_area :memo,:rows=>2,:title=>"可以为空" %>
    </p>
    <p>
      <%= form.submit "保存",:disable_with=>"保存…",:disabled => false ,:class=>"button"%> <%= link_to_close_lightbox "取消"  %>
    </p>

  <%end%>
</div>